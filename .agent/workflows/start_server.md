---
description: Start the Vini application server
---

1. Open a terminal and navigate to the server directory:
   ```
   cd "c:/VINI anti/vini/server"
   ```
2. Install dependencies:
   ```
   npm install
   ```
3. Start the server in development mode:
   ```
   npm run dev
   ```
