---
description: Start the Vini application stack (Backend + Frontend)
---

1. Start the Backend Server:
   ```cmd
   start cmd /k "cd /d "c:/backup/VINI anti/vini/server" && npm run dev"
   ```

2. Start the Frontend Web App:
   ```cmd
   start cmd /k "cd /d "c:/backup/VINI anti/vini" && npm run dev"
   ```
